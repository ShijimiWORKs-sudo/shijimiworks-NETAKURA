# テスト報告

# テスト報告

## 2026-07-06 表記統一・ロゴ修正後テスト

### 実行環境

- Node.js: v24.18.0
- npm: 11.16.0
- 開発サーバー: `http://localhost:3001`
- `3000` 番ポートは別アプリが使用中

### コマンド結果

- `npm install`: 成功
- `npm run typecheck`: 成功
- `npm run build`: 成功
- `npm run dev`: 成功。`http://localhost:3001` で起動
- GitHub push: 成功
- Vercel CLI: 未インストールのため、この環境では直接確認不可

### 表記確認

- ヘッダーの表示名を `NETAKURA` に統一
- ホーム画面の見出しを `NETAKURA` に統一
- metadata title を `NETAKURA` に統一
- README、docs、法務ページのユーザー向け名称を `NETAKURA` に統一
- カタカナ表記、読み補足、旧名称はユーザー向け表示から削除

### ロゴ確認

- ヘッダーに `public/images/logo001_02.png` を表示
- alt は `NETAKURAロゴ`
- PCヘッダーのロゴ高さは 58px
- スマホヘッダーのロゴ高さは 42px
- ヒーローエリアのロゴ高さは 84px〜116px
- `object-fit: contain` を使用
- ロゴとテキストの重なりなし
- PC幅: ヘッダー58px、ヒーロー116px
- スマホ幅: ヘッダー42px、ヒーロー84px、横スクロールなし

### 背景色確認

- 全体背景を淡いミントグレー基調に調整
- ヒーローエリアに薄いミントから白のグラデーションを追加
- カード背景と本文コントラストを維持

### 生成品質確認

入力:

```text
OLDという絵映画を感想として見たけど何を書けばいいかわからない
```

ジャンル:

```text
映画
```

確認結果:

- `映画『OLD』を見たあと、感想を書くための切り口` が出る
- `ただ面白かったで終わらせない映画感想の作り方` が出る
- `映画の感想をnote記事に変えるための3つの視点` が出る
- `して見たけど何を発信ネタに変えるための3つの視点` は出ない
- `書けばいいか分からを手がかり` のような不自然な差し込みを除去
- 映画ジャンルで、感想、考察、印象に残った場面、テーマ、登場人物、映像表現、note記事化、SNS投稿化を反映

### 保存・一覧・詳細・削除・コピー

- ブラウザ操作で生成結果表示: 成功
- Markdownコピー: 成功
- 設定画面のポイント消費案日本語表示: 成功
- 保存処理: localStorage / sessionStorage / Cookie フォールバックとして確認
- 保存済み一覧: ブラウザ内保存から読み戻す実装を維持
- 詳細表示: ブラウザ内保存から対象IDを取得する実装を維持
- 削除: ブラウザ内保存から対象IDを除外する実装を維持

### 注意

ブラウザ自動操作セッションが確認ダイアログ周辺でタイムアウトすることがあった。実装上、保存済み一覧と詳細画面は `loadSavedIdeas` / `loadSavedIdea` でブラウザ内保存から読み戻す。

