# テスト報告

## 2026-07-07 ロゴのみブランド表示への調整

### 実行環境

- 開発サーバー: `http://localhost:3001`
- `3000` 番ポートは別プロセスが使用中

### コマンド結果

- `npm run typecheck`: 成功
- `npm run build`: 成功
- `npm run dev`: 成功。`http://localhost:3001` で起動
- GitHub push: 成功
- Vercel CLI: 未インストールのため、この環境では直接確認不可

### 表示方針の確認

- 通常UIではロゴ画像のみでブランド表示する方針に変更
- ヘッダー横の `NETAKURA` テキストを削除
- ホーム画面ヒーローの `NETAKURA` テキスト見出しを削除
- alt を `サービスロゴ` に変更
- フッターを `© 2026 ShijimiWORKS` に変更
- 旧名称、読み補足、カタカナ表記は通常UIに表示されないことを確認
- metadata、README、docs、法務本文、内部識別子では必要な正式名称のみ維持

### ロゴ確認

- ヘッダーに `public/images/logo001_02.png` を表示
- PCヘッダーのロゴ高さは 58px
- スマホヘッダーのロゴ高さは 42px
- ヒーローエリアのロゴ高さは 84px〜116px
- `object-fit: contain` を使用
- ロゴとナビゲーション、見出し、説明文の重なりなし

### 背景色確認

- 全体背景は淡いミントグレー基調を維持
- ヒーローエリアの薄いグラデーションと余白を調整
- ロゴと見出しが近すぎないよう余白を拡大

### 生成品質確認

入力:

```text
OLDという映画を見た。面白かったけど、感想として何を書けばいいかわからない。
```

ジャンル:

```text
映画
```

出力形式:

```text
まとめて生成
```

確認結果:

- 映画ジャンルの生成結果が表示される
- 感想、考察、印象に残った場面、テーマ、登場人物、映像表現、note記事化、SNS投稿化の観点を含む
- 不自然な日本語連結を避けるテンプレートを維持

### 保存・一覧・詳細・削除・コピー

- 新規作成画面を表示できる
- 生成できる
- 保存できる
- 保存済み一覧を開ける
- 詳細画面を開ける
- コピーできる
- Markdownコピーできる
- 削除処理は確認ダイアログ経由で実行される実装を維持
- 設定画面のポイント消費案は日本語表示を維持

### 法務ページ

- `/terms`: 表示確認
- `/privacy`: 表示確認
- `/disclaimer`: 表示確認
- `/contact`: 表示確認
- `/operator`: 表示確認
- `/legal`: 表示確認

### 注意

- ブラウザ自動操作では確認ダイアログ周辺が不安定になることがあるため、削除は実装経路と表示上の導線をあわせて確認する
- Vercel は CLI がないため、GitHub push 後にダッシュボードで自動デプロイ結果を確認する
